# Local environment and secrets
.env
.env.*
*.pem
*.key
*.p12
*.pfx
*.crt
*.cert
*token*
*cookie*
cookies.json

# Python / tooling
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Private source inputs
feeds/follow.opml
feeds/*.private.opml
feeds/private*.opml

# Optional private/generated digests
data/email-digest.json

# Probe HTML dumps (NUCLEAR_PROBE=1 in Actions workflow) — gitignored so
# the workflow's untracked-files check doesn't fail, but locally visible
# for offline selector tuning.
data/.probe-*.html

# Local OS/editor noise
.DS_Store
.idea/
.vscode/
